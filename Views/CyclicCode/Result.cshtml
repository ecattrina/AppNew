@model AppNew.Models.CyclicCodeResultModel
@{
    ViewData["Title"] = "Результаты кодирования";
}

<div class="container mt-4">
    <h2 class="mb-4">Результаты кодирования в циклическом коде</h2>

    <div class="card mb-4 border-info">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0">Алгоритм кодирования</h5>
        </div>
        <div class="card-body">
            <p>Циклический код (n, k) с порождающим многочленом g(x) степени r = n - k.</p>
            <p><strong>Пошаговый алгоритм:</strong></p>
            <ol>
                <li><strong>Записать информационный полином m(x)</strong> (степени k-1) из информационной комбинации</li>
                <li><strong>Умножить m(x) на x<sup>r</sup></strong> - сдвиг информационных битов влево на r позиций</li>
                <li><strong>Разделить полученный полином на g(x)</strong> и найти остаток r(x) (деление выполняется по модулю 2)</li>
                <li><strong>Сформировать кодовую комбинацию:</strong> c(x) = x<sup>r</sup> · m(x) + r(x), где остаток добавляется в младшие разряды</li>
            </ol>
            <p class="mb-0"><small class="text-muted">Все операции выполняются в поле GF(2), где сложение и вычитание эквивалентны операции XOR</small></p>
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <h5>Параметры кода</h5>
        </div>
        <div class="card-body">
            <ul class="list-unstyled">
                <li><strong>Общая длина кода (n):</strong> @Model.CodeLength</li>
                <li><strong>Информационные биты (k):</strong> @Model.DataBits</li>
                <li><strong>Контрольные биты (r):</strong> @Model.ParityBits</li>
                <li><strong>Порождающий многочлен:</strong> g(x) = @Model.GeneratorPolynomial</li>
            </ul>
        </div>
    </div>

    @foreach (var encoding in Model.EncodingResults)
    {
        <div class="card mb-4">
            <div class="card-header">
                <h5>Комбинация: @encoding.InformationCombination</h5>
            </div>
            <div class="card-body">
                <div class="mb-4">
                    <h6 class="mb-3">Пошаговое решение:</h6>
                    @foreach (var step in encoding.Steps)
                    {
                        <div class="mb-3 p-3 bg-light rounded">
                            <div class="fw-semibold mb-2 text-primary">@step.Description</div>
                            <div class="mb-2 text-muted">@step.Calculation</div>
                            <div class="text-success fw-semibold">@step.Result</div>
                        </div>
                    }
                </div>

                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead class="table-dark">
                            <tr>
                                <th>Этап</th>
                                <th>Полином</th>
                                <th>Бинарное представление</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td><strong>Информационный полином m(x)</strong></td>
                                <td>@encoding.InformationPolynomial</td>
                                <td><code>@encoding.InformationCombination</code></td>
                            </tr>
                            <tr>
                                <td><strong>x<sup>r</sup> · m(x)</strong></td>
                                <td>@encoding.ShiftedPolynomial</td>
                                <td><code>@(encoding.InformationCombination + new string('0', Model.ParityBits))</code></td>
                            </tr>
                            <tr>
                                <td><strong>Остаток r(x)</strong></td>
                                <td>@encoding.RemainderPolynomial</td>
                                <td><code>@encoding.Remainder</code></td>
                            </tr>
                            <tr class="result-table-final">
                                <td><strong>Кодовая комбинация c(x)</strong></td>
                                <td><strong>@encoding.CodewordPolynomial</strong></td>
                                <td><div class="result-code">@encoding.Codeword</div></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    <div class="mb-3 d-flex flex-column flex-md-row gap-2">
        <a asp-action="Index" class="btn btn-primary">Новый расчет</a>
        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Выбор задачи</a>
    </div>
</div>
